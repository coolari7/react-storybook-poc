import React from "react";
import { Label } from "./Label";

export default {
  title: "UI/Forms/Label",
  component: Label,
  args: {},
};

export const Default = (args) => {
  return <Label text="First Name" />;
};
